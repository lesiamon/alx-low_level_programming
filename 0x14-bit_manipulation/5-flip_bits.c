#include "main.h"
unsigned int flip_bits(unsigned long int p, unsigned long int k)
{
	unsigned long int xor = n ^ m, bits = 0;

	while (xor > 0)
	{
		bits += (xor & 1);
		xor >>= 1;
	}

	return (bits);
}
